const compte = document.querySelectorAll(".compte");
const panier = document.querySelectorAll(".panier");

compte.addEventListener("click", function () {
  document.location.href = "http://localhost/Rhuma_sug/compte.php";
});
